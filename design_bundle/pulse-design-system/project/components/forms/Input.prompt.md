Pulse text field — hairline border, focus deepens to ink.

```jsx
<Input label="Home base" placeholder="Search a city or address" iconLeft={<Pin/>} />
```

Sizes `sm | md | lg`. Use for search, location, preference values.
