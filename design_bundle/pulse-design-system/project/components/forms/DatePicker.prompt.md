Compact month calendar for date search — single date, month nav, today ringed, selected fills ink.

```jsx
<DatePicker value={date} onChange={setDate} />
```

Pair with quick date chips (Today / This weekend) in search and filters.
