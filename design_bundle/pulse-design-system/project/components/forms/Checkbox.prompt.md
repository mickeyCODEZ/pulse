Tap-to-toggle pill — interest tags & dealbreakers in onboarding/preferences. Selected inverts to ink + check.

```jsx
<Checkbox checked={on} onChange={setOn} label="Live music" />
```

Lay out in a wrapping flex row with `gap`.
