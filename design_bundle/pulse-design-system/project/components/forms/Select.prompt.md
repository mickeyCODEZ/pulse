Styled native select for sort order, distance units, digest frequency.

```jsx
<Select label="Sort" value={sort} onChange={e=>setSort(e.target.value)}
  options={["For you","Soonest","Nearest","Cheapest"]} />
```
