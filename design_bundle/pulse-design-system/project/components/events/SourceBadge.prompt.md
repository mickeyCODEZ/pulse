Source attribution + dedup. First source is "via X"; extras render as a dashed "also on …" chip — the transparency the aggregation depends on.

```jsx
<SourceBadge sources={["Resident Advisor","DICE","Eventbrite"]} />
```
