Relevance chip — the "why this surfaced" signal. Radar glyph + matched reasons, optional score.

```jsx
<RelevanceChip reasons={["live music","under $15"]} score={94} />
```

Every card in the ranked feed should carry one — it's what makes high volume feel curated.
