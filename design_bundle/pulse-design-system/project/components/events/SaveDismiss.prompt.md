Save / dismiss pair. Dismiss is deliberately lightweight — it teaches the ranker, not deletes. Use `overlay` on imagery.

```jsx
<SaveDismiss saved={saved} onSave={save} onDismiss={dismiss} overlay />
```
