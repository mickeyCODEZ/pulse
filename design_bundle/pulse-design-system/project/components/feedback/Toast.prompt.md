Transient ink notice for save/dismiss/calendar confirmations, with optional undo.

```jsx
<Toast message="Saved to your list" actionLabel="Undo" onAction={undo} />
```

Mount briefly (~4s) bottom-center on mobile, above the bottom nav. You control timing.
