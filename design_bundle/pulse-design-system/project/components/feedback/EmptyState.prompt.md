Empty / cold-start / quiet-city / error state — line-map glyph, serif headline, action. Never leave a blank screen.

```jsx
<EmptyState glyph="quiet" title="A quiet week in Reykjavík"
  body="Only a few events match right now. We'll ping you when more drop."
  action={<Button variant="secondary">Widen my filters</Button>} />
```

Glyphs: `map | search | saved | quiet | error`.
