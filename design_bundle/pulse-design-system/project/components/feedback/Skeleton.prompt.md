Shimmer loading placeholder; `EventCardSkeleton` is the prebuilt card variant for cold-start & infinite scroll.

```jsx
<EventCardSkeleton />
<Skeleton width="40%" height="12px" />
```

Honors prefers-reduced-motion. Show 3–6 while a city loads.
