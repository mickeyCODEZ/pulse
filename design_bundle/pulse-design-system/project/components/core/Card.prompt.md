Generic surface container — hairline border, restrained elevation.

```jsx
<Card interactive>…</Card>
<Card inverse>Hidden gem…</Card>
```

`interactive` lifts on hover (clickable cards). `inverse` flips to the ink surface — the editorial "Hidden gems" treatment. Don't stack high elevations; prefer borders.
