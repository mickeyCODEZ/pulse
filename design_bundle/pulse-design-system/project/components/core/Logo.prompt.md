Brand lockup — the map-pin/radar mark with the optional "Pulse" serif wordmark; inherits `currentColor`.

```jsx
<Logo size={30} />
<Logo size={22} showWordmark={false} />
```

Place in headers and onboarding. On dark surfaces it flips automatically via `currentColor`; set `color` to force a value.
