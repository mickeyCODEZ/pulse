Icon-only control for toolbars and card actions (save, dismiss, share, more).

```jsx
<IconButton label="Save" active={saved}><Bookmark/></IconButton>
<IconButton label="Dismiss" variant="outline"><X/></IconButton>
```

`active` shows the toggled state. Keep icons 18–20px. Use `round` for floating actions.
