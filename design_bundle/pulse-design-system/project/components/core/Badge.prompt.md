Small status/count badge. Mostly monochrome; tones carry minimal chroma for feedback only.

```jsx
<Badge tone="inverse">3 new</Badge>
<Badge dot tone="ok" />
```

Use `dot` for unread indicators on nav. Reserve `ok|warn|error` for system feedback, not content.
