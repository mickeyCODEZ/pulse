# Pulse frontend config (optional — defaults work for local dev).
# Point at the backend; defaults to http://localhost:8000.
VITE_API_URL=http://localhost:8000
# Only if the backend sets APP_TOKEN (single-user gate).
VITE_APP_TOKEN=
