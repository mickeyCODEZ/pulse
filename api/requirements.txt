# Full Vercel deploy (Postgres + all civic/scraping sources). When flipping the
# backend to the stateful app: copy this over requirements.txt, point vercel.json
# at full.py, ensure DATABASE_URL (Neon pooled) is set, then deploy.
fastapi>=0.115
httpx>=0.27
sqlmodel>=0.0.22
pydantic-settings>=2.6
python-dateutil>=2.9
psycopg[binary]>=3.2
icalendar>=6.0
feedparser>=6.0
extruct>=0.17
selectolax>=0.3
sentry-sdk[fastapi]>=2.0
